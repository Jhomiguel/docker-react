sudo: required
services:
  - docker

before_install:
  - docker build -t jhomiguel/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jhomiguel/docker-react npm run test -- --coverage
